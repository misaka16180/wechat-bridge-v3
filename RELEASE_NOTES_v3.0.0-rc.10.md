# WeChat Bridge v3.0.0-rc.10

这是 Windows 电脑网页端的发布候选版。它使用可见桌面图像定位、普通鼠标键盘和 Windows
剪贴板完成微信消息发送，不提供移动端界面，不使用 OCR、微信进程注入、进程 Hook、私有协议或微信数据库。

## 本版重点

- v2 风格的电脑网页控制台，桥接服务与微信自动化独立启停。
- WeFlow SSE、AstrBot OneBot v11 和 Agent 主动发送 API。
- 文字、图片、文件、群聊真实 `@` 发送及严格的提交后禁止自动重发边界。
- OpenCV 组合式图像定位、DPI 自动适配、窗口布局缓存与多候选歧义保护。
- 识别工作区支持查看完整原图、全部候选、组合关系、临时调整候选阈值和保存本机适配图片。
- 托盘鼠标唤醒、可选窗口位置/大小、鼠标键盘输入保护和 `Esc` 停止快捷键。
- 合格的 Python 环境配置向导，以及 Python、pip、导入和运行异常保留机制。

## 下载文件

| 文件 | 大小（字节） | SHA-256 |
| --- | ---: | --- |
| `wechat-bridge-v3.0.0-rc.10-source.zip` | 330703 | `ace419ad85fbecbb3bd7093f6660ade017eb335375974d35b908caef40e98df7` |
| `wechat-bridge-v3.0.0-rc.10-dependencies-win64.zip` | 177700696 | `4e80b94902e3d5ee29e5306a477f819d2d32507618670001c778391494f76ebd` |
| `wechat-bridge-v3.0.0-rc.10-integrated-win64.zip` | 178033267 | `70592c9e7544d2ace7af3c7439677fa283fc90f3d5ae9c369eaee1d2a9af60c1` |

依赖包和整合版适用于 Windows x64、Python 3.10–3.14，不包含 Python 解释器。

## 验证边界

- v3 全量 305 项自动化测试通过。
- 发布白名单、ZIP 路径、内置 SHA-256 清单、首次配置、登录和启动错误保留检查通过。
- 31 个离线 wheel 已在全新临时环境中强制使用 `--no-index` 安装，OpenCV、NumPy、Pillow、Requests 和 WebSockets 导入通过。
- 当前自动化程序已完成本机现场测试；WeFlow → AstrBot → 微信完整外部链路尚未在发布环境中现场验收。

## 已知限制

- 只支持浅色模式与标准字体。
- 同名会话和同名群成员不做文字识别消歧。
- 微信界面大版本更新后可能需要重新检查或更新模板。
- `sent_unverified` / `media_sent_unverified` 表示发送动作已经提交但未把聊天气泡作为送达证明，调用方不得据此自动重发。

首次使用前请完整阅读[《使用说明》](使用说明.md)，并在安全测试会话中完成文字、图片、文件、群聊 `@` 和停止操作验收。
