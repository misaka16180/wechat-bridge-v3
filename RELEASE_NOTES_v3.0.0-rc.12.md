## v3.0.0-rc.12

这是 Windows 电脑网页端发布候选版。rc.12 把进入微信会话的默认方案从“点击固定搜索结果区域”改为纯键盘快捷操作，并增强了微信窗口被其它软件遮挡时的任务栏恢复兜底。

### 本次更新

- 输入会话名称并等待搜索结果刷新后，默认按一次上方向键，再随机等待 `0.2–0.5` 秒，最后按 Enter 进入微信当前排列的最上方结果。
- 默认流程不再定位或点击固定搜索结果区域，避免搜索页面出现其它卡片或内容时点错目标。
- 原有鼠标点击方案保留为需要用户明确选择的“不稳定备用方案”；快捷键失败时不会自动降级为鼠标点击。
- 自动化配置新增“进入会话方式”和“按上方向键后等待时间”，文字、图片、文件、调试发送与 Agent 主动发送 API 使用同一设置。
- 执行日志和主动 API 进度会分别记录搜索刷新等待、上方向键、随机等待实际值和 Enter 动作。
- 微信窗口已存在但被其它软件遮挡、普通前台激活持续被 Windows 拒绝时，程序会尝试单击唯一微信任务栏按钮，再继续确认窗口是否真正位于前台。
- 任务栏按钮与通知区域图标严格分开识别，避免托盘唤醒流程误点任务栏按钮而把微信再次最小化。

### 下载

| 文件 | 大小（字节） | SHA-256 |
| --- | ---: | --- |
| `wechat-bridge-v3.0.0-rc.12-source.zip` | 338097 | `eabc480e2403df9e0c19d70034ab9b191599d7585c2760fe675b927665e0ec06` |
| `wechat-bridge-v3.0.0-rc.12-dependencies-win64.zip` | 177700698 | `686329b014390d0e5b8525b15263f2252651bd5216b11f809dd7950a6a6f7a74` |
| `wechat-bridge-v3.0.0-rc.12-integrated-win64.zip` | 178040663 | `375e47ce3f39d8d76ecf1ebd525b1d45dc7ecb01bc3a408ae1e3a95825a32909` |

国内镜像：

- 仅环境包：[夸克网盘](https://pan.quark.cn/s/786afbc34739)，提取码 `5HSf`
- 整合包：[夸克网盘](https://pan.quark.cn/s/fdafe9b0af54)，提取码 `G666`
- GitHub 源码镜像：[夸克网盘](https://pan.quark.cn/s/ab90b8029f4b)，提取码 `CtvM`

网盘中的文件需要由维护者更新到 rc.12 后才与本页一致。使用镜像时请核对版本、大小和 SHA-256；校验不一致的压缩包不要运行。

### 验证结果

- `324` 项自动化回归全部通过。
- Python `compileall` 通过。
- 发布白名单检查通过：`69` 个产品文件。
- 源码包隔离首次运行检查通过：`70` 个 ZIP 文件，包含 `69` 个产品文件和一份生成的 SHA-256 清单。
- 仅环境包和整合包离线安装检查通过：`31` 个 wheel，覆盖 Windows x64 / Python 3.10–3.14，pip 强制 `--no-index`。

真实微信发送操作仍由使用者在自己的测试会话中验收；离线测试和网页代码检查不等于完整 WeFlow → AstrBot → 微信现场链已经通过。
