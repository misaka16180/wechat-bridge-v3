# WeChat Bridge v3 使用说明

当前版本：`3.0.0-rc.14`。这是 Windows 电脑网页端发布候选版，不提供移动端界面。

## 1. 使用范围

本程序把 WeFlow 主动推送的微信消息转换为 OneBot v11 事件交给 AstrBot，
再通过可见的 Windows 微信窗口完成文字、图片、文件和群聊 `@` 发送。

消息发送链只使用截图、图像定位、普通鼠标点击、键盘输入和 Windows 剪贴板；
不使用 OCR、pywinauto、微信进程注入、微信进程 Hook、私有微信协议或微信数据库。
只有可选的“从系统托盘唤醒微信”会只读获取 Windows 任务栏按钮的名称和矩形，再用普通鼠标
单击唯一微信图标；不会读取微信内部元素，也不会调用 UIA 的按钮执行动作。

## 2. 首次运行

发布方可能同时提供 GitHub 源码版、网盘完整依赖包和网盘整合版。源码版与依赖包的合并方式、
离线校验及适用 Python 版本见《离线依赖与整合版说明》。整合版已经带齐 Python 包，但仍需要
电脑安装 64 位 Python 3.10–3.14。

压缩包中只有两个 BAT 入口：

1. 准备 Python 3.10 或更高版本，然后双击 `first.bat` 打开运行环境配置向导。
2. 向导先显示当前找到的 Python 路径，但此时尚未检测。输入编号选中当前 Python，或输入另一套已有 Python 后，程序才检测版本和依赖，并展示结果供你继续确认。也可以选择创建或修复独立环境，或者退出。直接回车不会替你选中任何环境。
3. 选中已有 Python 时，可以输入 Python 所在文件夹，例如 `D:\Python`，也可以输入完整文件路径，例如 `D:\Python\python.exe`；两种路径均可带英文双引号。输入文件夹后，向导会显示最终解析出的解释器路径；如果文件夹内存在多个解释器，会要求用户明确选中其中一个。检测通过后仍会显示操作计划，并等你确认、重选、返回或退出，不会因为检测成功就自动保存。
4. 已有 Python 缺少依赖时，向导不会改动它。你可以明确选择用它引导一个独立环境，也可以换一套 Python、返回或退出。
5. 创建独立环境时只填写目标文件夹，例如 `D:\WeChatBridgeRuntime`。向导会在执行前列出引导 Python、目标目录、新建或修复状态、pip 和七项依赖；确认后才创建目录并安装。若误填 `python.exe` 或一个已经直接包含 `python.exe` 的 Python 安装目录，可自行决定转到“选中已有 Python”流程或重新填写，不会直接向该目录安装。
6. 配置成功后，双击 `start.bat` 启动程序。

解释器位置只记录在本机的 `.runtime-python.txt`，不会进入发布包。
输入的 Python 文件、目录、引号和 Windows 非法字符会在创建环境前检查；错误路径会要求重新输入，
不会再直接交给 `venv` 后才报错。确认前选择退出、关闭输入或按 `Ctrl+C` 会以取消码 `130` 结束，
不会保存新的解释器、创建目录或运行 pip。
Python 无法启动、pip 安装失败、依赖导入失败或程序运行异常时，控制台会显示退出码和
完整 traceback，并等待用户确认，不会直接闪退。

## 3. 微信必须完成的设置

开始自动化前，请在 Windows 微信中确认：

1. 设置 → 通用 → 字体大小：`标准`。
2. 设置 → 通用 → 主题色：`浅色模式`。
3. 设置 → 快捷键 → 发送消息：`Ctrl+Enter`。

同时保持微信已经登录、窗口可以恢复到桌面，并避免在自动化运行期间主动操作鼠标键盘。

## 4. 登录电脑网页控制台

首次运行会在控制台窗口显示网页地址、用户名和临时密码。未登录时服务器只返回登录页，
不会把管理台内容发送给浏览器。首次登录必须设置新密码。

登录并完成设置加载后，网页会显示一次“使用前必读”，集中核对标准字体、浅色模式、
`Ctrl+Enter` 和 DPI 图像比对策略。可以勾选“下次不再提示”；该选择只保存在当前浏览器，
以后仍可到“自动化配置 → 全局运行环境”重新设置 DPI。

默认只监听 `127.0.0.1`。如果开启局域网访问，请同时配置可信网络、Token 和 TLS；
在不可信网络中使用无鉴权 HTTP 会暴露控制能力。

## 5. 推荐配置顺序

1. 在“连接设置”中填写 AstrBot WebSocket 和 WeFlow 地址、Token，再启用连接。

如果 Agent 需要主动向微信发送消息，而不是等待 WeFlow 推送后由 AstrBot 回复，可在“连接设置 → 主动发送 API”启用机器接口。它不要求启动桥接服务，也不依赖 AstrBot、WeFlow 在线；只要网页控制台仍在运行、主动 API 已启用且微信自动化已启动，就可以发送。停止桥接不会取消主动 API 队列，停止微信自动化才会拒绝或取消任务。建议配置 Token；留空时接口只接受本机回环地址调用。Token 不限制长度：可手动填写，也可随机生成；生成后可显示和复制，保存成功后会在当前页面继续保留，刷新整个页面后才清空。服务端只返回“是否已配置”，不会把已保存 Token 重新传回网页。每个请求必须携带唯一 `request_id`，提交后查询任务状态，避免网络重试造成重复发送。图片和文件应先上传到 `/api/v1/uploads` 获取一次性 `upload_id`，再创建消息任务；这样不受普通 JSON 的 64 KiB 上限影响，也不需要给 Agent 开放任意本地目录读取权限。文字、图片、文件和当前 v3 的真实 @ 策略都复用同一套可见桌面自动化。完整字段和示例见 `主动发送API.md`。如需让 Agent 直接读取说明，可开启默认关闭的“公开 API 文档（无需登录）”；保存后提供 `/docs/active-api` 和 `/docs/active-api.md`，若控制台监听局域网，同网段设备也能读取，因此只在需要时开启。公开页是固定文档，不包含 Token、连接状态、任务或日志。

主动 API 使用异步顺序队列：HTTP 提交立即返回任务编号，多个请求按提交顺序逐条执行，状态接口返回排队位置、阶段百分比和逐步日志。“收到消息至开始发送的最短间隔”只作用于 WeFlow → AstrBot 被动回复，不作用于主动 API 或自动化调试。
2. 在“自动化配置 → 全局运行环境”核对 DPI、托盘唤醒、停止快捷键和可选窗口外框。
3. 在“自动化配置”中选择实际运行配置。
4. `均衡`、`稳妥`、`快速` 是只读模板；只有 `自定义` 可以编辑和保存。
5. 查看模板、编辑自定义和切换实际运行配置互不影响。
6. 如果短语输入后经常弹出微信表情候选层，可在自定义配置的“寻找与输入”中开启“输入结束后在消息末尾增加一个换行”。它默认关闭；开启时必须按弹窗要求确认微信发送快捷键已经设为 `Ctrl+Enter`。
7. 在首页分别启动桥接程序和微信自动化程序。
7. 先在“自动化调试”中向安全测试会话发送短文本，再测试图片、文件和群聊 `@`。

调试任务可以通过网页按钮或默认 `Esc` 停止。停止微信自动化不会断开 WeFlow 或 AstrBot；
停止桥接程序才会停止连接和消息处理。按下停止快捷键后，网页会弹窗提示“自动化进程已停止”，
需要在首页“微信自动化程序”板块重新启动。自动化停用时，调试发送卡片会显示一层可关闭的说明，
其中也提供“立即启动自动化”入口；它与首页按钮控制同一个状态，不会出现两套互相冲突的开关。

### DPI 与自动比对范围

程序使用 Windows `GetDpiForWindow` 读取微信窗口 DPI；这个操作不截图、不激活、不点击微信。

- `自动识别并补充比对`：先只使用 Windows 报告的比例。只有锚点组合定位失败，才扫描补充范围；默认 `70%–150%`、间隔 `5%`。范围可填写 `50%–300%`，扫描间隔可填写 `1%–50%`；数值越小，比对越细，但首次完整定位可能越慢。
- `只使用手动比例`：只使用填写的固定比例，不扫描自动范围。适合 Windows 报告值与实际渲染比例不一致、且已经人工验证正确比例的环境。
- 自动检测失败不会阻止控制台加载。真实自动化任务恢复并准备微信窗口后会重新读取；仍无法读取时会明确报错，不会盲目点击。
- 搜索框、鼠标备用方案使用的搜索结果基准、发送按钮＋表情按钮组合、聊天输入区和返回按钮都使用同一套 DPI 策略，模板、ROI、几何距离与偏移量同步缩放。默认纯键盘 / 快捷键进入会话不会定位或点击搜索结果区域。

### 可选：固定微信窗口外框

该设置位于“自动化配置 → 识别比例与微信窗口”卡片，不属于均衡、稳妥、快速或自定义发送模板：

1. 先把微信从最小化状态恢复，拖到希望的位置并调整到希望的大小。
2. 回到网页点击“读取当前微信窗口”。程序只读取唯一可见微信主窗口的外框，不会点击、移动或输入微信。
3. 按需单独开启“固定微信窗口位置”和“固定微信窗口大小”。只开位置会保留当时窗口大小；只开大小会保留当时窗口位置。
4. 也可以直接手工填写 `X`、`Y`、`宽度`、`高度`。多屏环境允许负坐标。
5. 点击“保存全局运行环境”。以后每次操作微信前，程序会先应用已开启的项目；调整失败时会在输入或发送前停止，避免继续使用错误坐标。

Windows 或微信可能限制最小窗口尺寸，日志会记录请求值和实际外框。建议用户自行点击微信标题栏中、
最小化按钮左侧的图钉保持置顶；程序只提供提醒，不会替用户点击图钉或强制修改置顶状态。

## 6. 发送与重试边界

- 默认使用纯键盘 / 快捷键进入会话：输入会话名称并等待配置中的搜索刷新时间后，按一次上方向键，随机等待 `0.2–0.5` 秒，再按 Enter。鼠标点击第一条结果仅是不稳定备用方案，必须由用户明确选择，快捷键失败时不会自动改用鼠标。
- 普通文字可以选择剪贴板整段粘贴，或逐字随机间隔输入。
- 逐字模式默认开启“自然输入节奏”：每次随机连续输入 `2–6` 个字符，再随机停顿 `0.18–0.65` 秒；自定义配置可调整连续字数和停顿时间。搜索会话名称和真实 `@` 昵称仍只使用普通字符间隔。
- 选择逐字输入后，可以单独开启“长文本自动改用剪贴板”。默认阈值为 40 个字符；只有消息总长度严格大于阈值才切换普通文本，真实 `@` 不会被粘贴成伪造的普通文字。
- 可选的末尾换行会在所有文字和真实 `@` 完成后按一次 Enter，把光标移到新行并收起表情候选浮层；它不负责发送，最终仍由鼠标点击发送按钮。
- 图片使用 `CF_DIB`，文件使用 `CF_HDROP` 写入 Windows 剪贴板，再通过普通 `Ctrl+V` 粘贴。
- 真实 `@` 会等待微信候选框，守卫通过后只按一次 Enter。
- 真实 `@` 没有局部重试；默认失败时保留为普通文字并记录警告。
- 整体重试只允许发生在发送按钮点击前。
- 鼠标会沿带随机弯曲和轻微平滑扰动的路径移动，扰动在起点与最终点击点归零；最终坐标仍严格位于识别出的安全点击区内。这只能减少机械感，不能承诺降低账号风控或避免退出登录。
- 如果已经执行末尾 Enter，但随后无法重新找到可用发送按钮，程序会把状态视为“可能已经提前发送”并禁止整条消息重试，避免在微信仍使用 Enter 发送时重复刷屏。
- 一旦发送动作已经提交，程序绝不会因为没有观察到气泡而自动重发。
- `sent_unverified` 或 `media_sent_unverified` 表示发送动作已提交，但程序没有把气泡当作最终送达证明。

随机等待用于给微信界面留出响应时间并改善操作节奏，不保证降低账号风控。

## 7. 微信界面识别

这个工作区用于确认程序能否唯一找到微信搜索框和消息输入区域。通过表示画面具备基础定位条件，不代表完整发送流程已经现场通过。页面按用户任务分为三步：

- `检查是否可识别`：两种检查方式只选一种。日常使用默认检查这台电脑当前打开的微信；“检查另一台电脑的微信截图”只是替其他用户排查时的备用入口。当前微信检查会按调试发送相同的方式准备窗口，只进行截图识别，不点击微信内部、不输入、不发送。截图入口支持 PNG/JPEG/WebP、主动读取剪贴板或在粘贴区域按 `Ctrl+V`，必须填写截图来源电脑的 `50%–300%` 系统缩放比例。文件与剪贴板入口受 20 MiB、16384 单边像素和 4000 万总像素限制。
- `查看识别记录`：界面检查和调试发送会保存成功与失败记录；实际运行只保存失败记录。记录会列出原图、每个定位点的最佳尝试、所有过阈值候选、有效组合、安全点击区域和失败原因。
- `修复识别问题`：仅在出现识别失败时使用。页面只列出失败记录，并根据记录自动判断要修复搜索框还是消息输入区域。候选区会列出完整识别结果；如果目标未达到默认阈值，可以临时调低候选阈值重新加载。用户从候选中选择一组符合关系的参照元素后，页面逐项显示实际距离、默认范围和拟保存的本机适配范围。保存后的规则会被实际自动化使用，但只能在安全上限内调整几何范围，不能替换参照元素、组合关系或把规则无限放宽。可随时改回程序默认识别方式。

出现多个组合指向不同目标时，程序会停止点击并报告 `visual_target_ambiguous`。每条快照都可
导出“隐私诊断包”：默认只包含识别元数据和各定位点最佳尝试附近的小范围裁图，不包含
`original.png`、总览图或任何完整聊天窗口截图。完整原图仍只保存在桥接器本机的快照目录中。

## 8. 当前限制

- 只支持 Windows 电脑端微信浅色模式和标准字体。
- 不识别会话或群成员文字；默认快捷键方式和鼠标备用方式在同名时都会选择微信当前排列的第一项。
- 自动化依赖微信界面布局和模板，微信大版本更新后可能需要更新定位图片或几何规则。
- 首次对外使用前，应由使用者在自己的微信版本、DPI 和屏幕环境中完成现场验收。
- 固定位置/大小依赖当前 Windows 多屏坐标；显示器排列、缩放或分辨率改变后应重新读取并保存。

## 9. 出错时先看哪里

1. 不要关闭启动控制台，先复制最末尾的错误和 traceback。
2. 在网页“运行日志”中查看每一步操作和耗时。
3. 在“微信界面识别 → 查看识别记录”中查看失败原图、最佳尝试、候选和组合；需要反馈时优先导出隐私诊断包。
4. 确认微信处于浅色模式、标准字体，发送快捷键为 `Ctrl+Enter`。
5. 如果运行环境损坏，重新运行 `first.bat` 选择或修复环境。

发布包根目录中的 `RELEASE_MANIFEST.json` 记录所有产品文件的大小和 SHA-256，
可用于确认文件没有在传输或解压过程中损坏。
