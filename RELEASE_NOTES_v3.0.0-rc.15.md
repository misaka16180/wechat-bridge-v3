## v3.0.0-rc.15

这是 Windows 电脑网页端发布候选版。rc.15 重点修复搜索结果仍在刷新时容易误选的问题，并把进入会话的默认方案从不可靠的键盘选择改为可解释、可拒绝的视觉分组定位。

### 本次更新

- 默认进入会话方式改为视觉识别搜索分组：先用已验证的搜索框确定横向范围，再识别“最常使用 / 功能 / 联系人 / 群聊”等稳定标题。
- 同时出现多个合格分组时，按纵坐标排序并选择最上方分组，再点击该分组的第一项；暂不读取结果文字，同名会话仍以微信当前排序为准。
- 新增四类浅色模式分组标题模板及独立定位规则，模板、DPI 缩放、候选阈值、ROI 和几何约束统一参与判断。
- 找不到合格分组标题时会停止并保存识别快照，不再回退固定纵坐标、“搜索网络结果”、“聊天记录”或盲按 Enter。
- 纯键盘 / 快捷键进入会话保留为用户可选的兼容模式，不再作为默认方案，也不会在视觉定位失败时自动触发。
- “输入会话名称后等待”从通用界面稳定时间中拆分为独立随机范围；均衡、稳妥、快速、自定义及当前运行配置默认统一为 `0.5–0.7` 秒。
- 分组标题图像识别耗时计入本次随机等待预算；如果提前识别成功，程序仍会等预算结束后重新截图和验证，避免用加载中的旧画面坐标点击。
- 网页自动化配置增加紧凑的最短/最长范围输入、数值纠正、配置对比、摘要和逐步耗时日志；文本与媒体发送共用同一搜索等待策略。

### 下载

| 文件 | 大小（字节） | SHA-256 |
| --- | ---: | --- |
| `wechat-bridge-v3.0.0-rc.15-source.zip` | 363957 | `13922f43902a3b5c1067e1e5e80b6cc9d56d40efb0c25b1b82b575d501dc859d` |
| `wechat-bridge-v3.0.0-rc.15-dependencies-win64.zip` | 177700699 | `15c1eefa60388e77826c7ba44f0e86ab4c4165f0e351779a5148c829873915fb` |
| `wechat-bridge-v3.0.0-rc.15-integrated-win64.zip` | 178066524 | `12ed3cc6f478cd73c0f80764259d3b10ed19f0bc128794ec59381ce026fe9bc9` |

国内镜像：

- 仅环境包：[夸克网盘](https://pan.quark.cn/s/786afbc34739)，提取码 `5HSf`
- 整合包：[夸克网盘](https://pan.quark.cn/s/fdafe9b0af54)，提取码 `G666`
- GitHub 源码镜像：[夸克网盘](https://pan.quark.cn/s/ab90b8029f4b)，提取码 `CtvM`

网盘中的文件需要由维护者更新到 rc.15 后才与本页一致。使用镜像时请核对版本、大小和 SHA-256；校验不一致的压缩包不要运行。

### 验证结果

- `362` 项自动化回归全部通过。
- Python `compileall` 通过。
- 发布白名单检查通过：`75` 个产品文件。
- 源码包隔离首次运行检查通过：`76` 个 ZIP 文件，包含 `75` 个产品文件和一份生成的 SHA-256 清单。
- 仅环境包和整合包离线安装检查通过：`31` 个 wheel，覆盖 Windows x64 / Python 3.10–3.14，pip 强制 `--no-index`；实际导入 OpenCV `4.14.0`、NumPy `2.5.2`、Pillow `12.3.0`、Requests `2.34.2`、WebSockets `17.0.1`。
- 搜索分组排序、无标题安全拒绝、随机等待范围透传、识别耗时计入预算和等待后重新截图均有自动化测试覆盖。

真实微信发送操作仍由使用者在自己的测试会话中验收；离线测试、HTTP 测试和网页代码检查不等于完整 WeFlow → AstrBot → 微信现场链已经通过。
