## v3.0.0-rc.14

这是 Windows 电脑网页端发布候选版。rc.14 继续改善可见桌面自动化的稳定性与操作节奏，并补全可供外部 Agent 主动发送文字、图片和文件的异步 API、二进制上传流程及内置接入文档。

### 本次更新

- 聊天输入区的横向安全点击范围改为“表情按钮对称轴到发送按钮对称轴”，纵向仍保留上下各 20% 的安全边距，扩大可用区域且避免落到无效边缘。
- 新增“发送前检查停顿时间”：输入完成并识别到可用发送按钮后，先按配置的随机区间停顿，再点击发送；文字、图片、文件、自动化调试和主动发送 API 统一生效。
- 逐字输入增加自然节奏：可按随机字数组成输入片段，并在片段之间加入随机思考停顿；长文本仍可按阈值自动改用剪贴板粘贴。
- 鼠标移动采用带轻微扰动的弧线路径，点击前停顿和按下至抬起时长继续使用独立随机区间。
- 主动发送 API 不依赖 WeFlow/AstrBot 桥接，只依赖 v3 控制台、主动 API 开关和微信自动化；多个请求按队列顺序串行执行，并返回进度、步骤日志和最终状态。
- 主动发送 API 正式支持图片和文件二进制上传：先 `POST /api/v1/uploads` 获取一次性 `upload_id`，再创建消息任务，不受普通 JSON 64 KiB 上限影响，也不要求给 Agent 扩大本地目录白名单。
- 增加完整的 `主动发送API.md`：覆盖鉴权、幂等 `request_id`、异步轮询、排队并发、文字/媒体字段、取消、错误码、禁止重复发送边界和仅用标准库的 Python 示例。
- 控制台新增“公开 API 文档（无需登录）”开关，默认关闭。显式开启并保存后，Agent 可读取 `/docs/active-api` 网页版或 `/docs/active-api.md` 原始 Markdown；公开页只含静态说明，不包含 Token、配置、任务或日志。
- 公开文档开关纳入配置持久化；若控制台监听局域网，界面会明确提醒开启后同网段设备也能读取说明。

### 下载

| 文件 | 大小（字节） | SHA-256 |
| --- | ---: | --- |
| `wechat-bridge-v3.0.0-rc.14-source.zip` | 350582 | `c34c827783003c36c04d0fbf0dfc18b98b49fd57ced6f86aae8763c701b20717` |
| `wechat-bridge-v3.0.0-rc.14-dependencies-win64.zip` | 177700698 | `3e5b12858548c805bbe15a503872511ea860078509ea6e904a5c310f51bd2ef1` |
| `wechat-bridge-v3.0.0-rc.14-integrated-win64.zip` | 178053148 | `8626e01f4e13216a1778482f1f94a5d01bc95f32daea8422531291a60fd75380` |

国内镜像：

- 仅环境包：[夸克网盘](https://pan.quark.cn/s/786afbc34739)，提取码 `5HSf`
- 整合包：[夸克网盘](https://pan.quark.cn/s/fdafe9b0af54)，提取码 `G666`
- GitHub 源码镜像：[夸克网盘](https://pan.quark.cn/s/ab90b8029f4b)，提取码 `CtvM`

网盘中的文件需要由维护者更新到 rc.14 后才与本页一致。使用镜像时请核对版本、大小和 SHA-256；校验不一致的压缩包不要运行。

### 验证结果

- `352` 项自动化回归全部通过。
- Python `compileall` 通过。
- 发布白名单检查通过：`69` 个产品文件。
- 源码包隔离首次运行检查通过：`70` 个 ZIP 文件，包含 `69` 个产品文件和一份生成的 SHA-256 清单。
- 仅环境包和整合包离线安装检查通过：`31` 个 wheel，覆盖 Windows x64 / Python 3.10–3.14，pip 强制 `--no-index`。
- 主动 API 的文本任务、二进制媒体上传、一次性 `upload_id`、幂等、串行队列、取消、无桥接运行和公开文档默认关闭边界均有自动化测试覆盖。

真实微信发送操作仍由使用者在自己的测试会话中验收；离线测试、HTTP 测试和网页代码检查不等于完整 WeFlow → AstrBot → 微信现场链已经通过。
