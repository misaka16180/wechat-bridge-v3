# WeChat Bridge v3 发布候选现场验收清单

本清单只用于 `3.0.0-rc.12` 的 Windows 电脑端现场验收。请使用文件传输助手或专门测试群，
不要选择重要联系人或正式工作群。验收操作由使用者完成，程序不会代替使用者判断最终送达。

## 验收前

- [ ] 从候选 ZIP 解压到一个新的空目录，不覆盖 v2 或旧 v3。
- [ ] 微信已登录，字体大小为“标准”，主题为“浅色模式”。
- [ ] 微信发送快捷键为 `Ctrl+Enter`。
- [ ] 首次运行 `first.bat` 后窗口没有闪退；失败时能够看到 pip/Python 错误和退出码。
- [ ] `start.bat` 能打开控制台并显示网页地址与首次登录信息。
- [ ] 未登录时浏览器看不到管理台；首次登录后必须修改密码。
- [ ] 首次进入管理台会显示“使用前必读”，且可选择下次不再提示。
- [ ] 管理台能自动读取当前微信窗口 DPI；读取时不会激活、移动或点击微信。
- [ ] 自动 DPI 模式先使用 Windows 报告比例；定位失败后才在设置的补充范围内比对。
- [ ] DPI 扫描间隔默认 5%，可在 1%–50% 内调整；保存后下一次定位实际使用新间隔。
- [ ] 自动补充比对范围默认是 `70%–150%`，修改、保存并重新载入后仍保留；最小值大于最大值时会在失焦后纠正。
- [ ] 手动 DPI 模式只使用填写的固定比例；以后可在“自动化配置 → 全局运行环境”再次切换和设置。
- [ ] “微信界面识别 → 检查是否可识别”默认只展开“检查这台电脑上的微信”；两种方式明确为二选一。点击“检查当前微信界面”后按调试发送相同的方式准备微信窗口；检查搜索框和消息输入区域时不会点击微信内部、输入或发送。
- [ ] 主动发送 API 随机生成 Token 后可显示、隐藏和复制；保存成功后当前页面继续保留，后台刷新不会清空，刷新整个网页后才不再显示；短 Token 和长 Token 均可保存。
- [ ] 选择外部 PNG/JPEG/WebP 后只显示文件信息，不自动开始；确认截图所在电脑 DPI 并点击“检查这张截图”后才运行。
- [ ] “读取剪贴板”和粘贴区域 `Ctrl+V` 都能导入截图；浏览器拒绝主动读取时，粘贴后备入口仍可用。
- [ ] 离线导入截图时没有查找、激活、移动、点击或输入微信；结果与快照标明外部文件/剪贴板、DPI、文件名和真实格式。
- [ ] 检查结束后可按本次任务打开快照，并看到每个定位点的最佳尝试、过阈值候选和有效组合。
- [ ] “导出隐私诊断”生成的 ZIP 不含完整原图、总览图或组合整窗图片。

## 1. 桥接与自动化启停

- [ ] 首页能够单独启动、停止桥接程序。
- [ ] 首页能够单独启动、停止微信自动化程序。
- [ ] 停止自动化不会断开 WeFlow 或 AstrBot。
- [ ] 调试运行时网页“停止”按钮和默认 `Esc` 都能及时停止。
- [ ] 按下停止快捷键后，网页弹窗明确说明自动化已经停止，并提示到首页重新启动。
- [ ] 自动化停用时，调试卡片显示可关闭的悬浮引导；“立即启动自动化”和首页按钮控制同一个状态，启动后悬浮层自动消失。
- [ ] 微信缩到系统托盘后，“测试托盘唤醒”能打开隐藏图标区、只选择唯一“微信”按钮并通过普通鼠标单击恢复主窗口。
- [ ] “微信输入法”等相似名称不会被当作微信；出现多个微信候选时程序停止且不点击。

## 2. 私聊文字

- [ ] 向“文件传输助手”发送一条短文本。
- [ ] 剪贴板整段粘贴模式可以输入并只提交一次。
- [ ] 逐字输入模式的字符间隔不是固定值。
- [ ] 默认纯键盘 / 快捷键方式只点击搜索框；输入会话名称后按一次上方向键，随机等待 `0.2–0.5` 秒，再按一次 Enter 进入最上方结果。
- [ ] 默认方式没有定位或点击固定搜索结果区域；快捷键失败时也没有自动降级为鼠标点击。
- [ ] 手动选择“不稳定备用方案”后，程序才按搜索框基准推算并点击第一条结果区域。
- [ ] 日志包含查找、固定/随机等待及实际取值、快捷键或备用鼠标动作、输入和总耗时。
- [ ] “输入结束后在消息末尾增加一个换行”默认关闭。
- [ ] 开启末尾换行时，网页先明确询问并记录微信发送快捷键已设为 `Ctrl+Enter`；取消确认后选项自动关闭，未确认的配置无法保存。
- [ ] 开启后，所有文字和真实 `@` 输入完成才按一次 Enter，表情候选浮层消失，最终仍由鼠标点击发送按钮。
- [ ] 模拟末尾 Enter 后找不到发送按钮时，同一条消息不会进入整体重试；日志和结果明确提示可能已经提前发送。

## 3. 图片与文件

- [ ] 发送一张普通 PNG/JPG 图片，微信形成图片预览后只提交一次。
- [ ] 发送一个小型普通文件，微信形成文件卡片后只提交一次。
- [ ] 文件卡片不会被误识别成发送按钮。
- [ ] 粘贴尚未完成时，程序会继续等待启用状态发送按钮，而不是立即失败或重试。

## 4. 群聊真实 `@`

- [ ] 在专门测试群中输入一个没有同名冲突的成员昵称。
- [ ] 候选框出现后只按一次 Enter，并形成微信真实 `@`。
- [ ] 候选框超时时，开启降级会保留普通文字并记录警告。
- [ ] 关闭降级时会在发送前停止，再由整体重试策略决定是否从头尝试。
- [ ] 微信快捷键为 `Ctrl+Enter` 时，单独 Enter 不会误发送消息。

## 5. 窄聊天层与返回恢复

- [ ] 在存在发送栏和返回按钮的窄聊天层开始调试，会优先点击左上方返回按钮。
- [ ] 有多个返回按钮时按左上优先依次处理。
- [ ] 宽聊天窗口只有发送栏、没有返回按钮时不会误点返回。

## 6. 输入保护与布局缓存

- [ ] 鼠标锁定关闭时点击正常。
- [ ] 鼠标锁定开启时程序自己的点击正常，任务结束或停止后人的鼠标恢复。
- [ ] 键盘锁定开启后任务结束或停止时能够释放。
- [ ] 布局缓存开启时重复发送速度有所改善；移动、缩放微信窗口后会重新验证。
- [ ] 关闭布局缓存后仍能通过完整识别完成流程。

## 7. 可选窗口位置与大小

- [ ] “读取当前微信窗口”填入的是窗口外框 `X/Y/宽度/高度`，读取时没有点击、移动或输入微信。
- [ ] 只开启固定位置时，运行前移动到指定坐标，但保持当前大小。
- [ ] 只开启固定大小时，运行前调整到指定宽高，但保持当前位置。
- [ ] 两项都开启时，位置和大小同时应用；Windows 限制尺寸时日志记录实际外框。
- [ ] 关闭两项时程序不额外移动或缩放微信。
- [ ] 网页提示用户自行点击微信标题栏图钉，但程序没有自动点击或强制置顶。

## 8. 错误和识别快照

- [ ] 人为遮挡或改变微信状态后，错误信息指出失败步骤和原因。
- [ ] 调试成功与失败都有识别快照；实际运行失败有快照。
- [ ] 快照可以看到原图、候选、有效组合和安全点击区域。
- [ ] 多个有效组合指向不同目标时停止点击并报告 `visual_target_ambiguous`。
- [ ] 修复识别页面可以调低候选阈值加载更多候选，并完整显示未通过默认阈值的目标候选。
- [ ] 从候选中选择有效组合后，页面分别显示实际距离、默认范围和本机适配范围；保存后实际自动化使用该本机规则。
- [ ] 明显错误的候选组合和试图无限放宽的本机规则仍会被拒绝。

## 9. 完整链路

- [ ] WeFlow 主动推送白名单内的私聊消息能够转发到 AstrBot。
- [ ] WeFlow 主动推送白名单内的群聊消息能够按配置转发。
- [ ] 程序不会主动轮询会话补漏，也不会绕过 WeFlow 主动推送白名单。
- [ ] AstrBot 回复能够进入微信发送流程。
- [ ] 发送动作提交后，即使返回 `sent_unverified` 或 `media_sent_unverified` 也不会自动重发。

## 反馈时请提供

无需提供聊天正文或 Token。请提供：

1. 失败时间；
2. 日志中的错误代码和最后一段 traceback；
3. 识别快照编号；
4. 微信窗口是否移动/缩放、系统 DPI、微信版本；
5. 对应勾选项的通过/失败情况。
