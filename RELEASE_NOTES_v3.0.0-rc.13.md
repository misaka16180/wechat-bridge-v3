## v3.0.0-rc.13

这是 Windows 电脑网页端发布候选版。rc.13 为“逐字输入”增加可选的长文本自动切换策略：短消息继续按随机字符间隔输入，长消息的普通文本自动改用剪贴板粘贴，减少长段落逐字输入的耗时和异常感。

### 本次更新

- 自动化配置新增“长文本自动改用剪贴板”，作为“逐字输入”的从属选项，不增加第三种并列输入模式。
- 该选项默认关闭，默认阈值为 `40` 个字符；消息总长度 `≤ 40` 时继续逐字输入，严格大于 `40` 时才切换。
- 阈值按整条消息的总字符数判断；触发后只把普通文本片段改为剪贴板粘贴。
- 真实 `@` 仍使用原有键盘输入、候选框等待与 Enter 确认流程，不会被粘贴成普通文字 `@`。
- 自动化日志与主动发送 API 的任务日志会明确记录消息长度、阈值以及实际采用的输入策略。
- 控制台会根据输入方式和开关状态启用或禁用从属选项，并在配置摘要和模板对比中显示对应值。
- 旧配置没有新字段时保持关闭，升级后不会静默改变原来的输入习惯。

### 下载

| 文件 | 大小（字节） | SHA-256 |
| --- | ---: | --- |
| `wechat-bridge-v3.0.0-rc.13-source.zip` | 339176 | `bc96bcdc155f2104d644cadbe566cf221d70cea6fb1fe8278cc87696c48688e7` |
| `wechat-bridge-v3.0.0-rc.13-dependencies-win64.zip` | 177700699 | `fd7f250e9e62f5b4af77784f116f138d198a38e8cf2268e1b4ca5f9ead218ec5` |
| `wechat-bridge-v3.0.0-rc.13-integrated-win64.zip` | 178041743 | `69c050d0e516837d064af17990097d5bde49b5c939e8c5905d91e87163fb809e` |

国内镜像：

- 仅环境包：[夸克网盘](https://pan.quark.cn/s/786afbc34739)，提取码 `5HSf`
- 整合包：[夸克网盘](https://pan.quark.cn/s/fdafe9b0af54)，提取码 `G666`
- GitHub 源码镜像：[夸克网盘](https://pan.quark.cn/s/ab90b8029f4b)，提取码 `CtvM`

网盘中的文件需要由维护者更新到 rc.13 后才与本页一致。使用镜像时请核对版本、大小和 SHA-256；校验不一致的压缩包不要运行。

### 验证结果

- `338` 项自动化回归全部通过。
- Python `compileall` 通过。
- 发布白名单检查通过：`69` 个产品文件。
- 源码包隔离首次运行检查通过：`70` 个 ZIP 文件，包含 `69` 个产品文件和一份生成的 SHA-256 清单。
- 仅环境包和整合包离线安装检查通过：`31` 个 wheel，覆盖 Windows x64 / Python 3.10–3.14，pip 强制 `--no-index`。

真实微信发送操作仍由使用者在自己的测试会话中验收；离线测试和网页代码检查不等于完整 WeFlow → AstrBot → 微信现场链已经通过。
