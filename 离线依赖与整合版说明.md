# 离线依赖包与整合版说明

WeChat Bridge v3 同时提供三种文件，程序代码完全相同，区别只在是否携带 Python 依赖：

| 文件 | 建议发布位置 | 包含内容 | 适合谁 |
| --- | --- | --- | --- |
| `wechat-bridge-v*-source.zip` | GitHub | v3 程序源码和两个启动入口 | 网络访问 Python 软件源正常，或愿意另下依赖包的用户 |
| `wechat-bridge-v*-dependencies-win64.zip` | 网盘 | 全部直接与间接 Python 依赖，不含程序源码 | 已从 GitHub 下载源码、但安装依赖很慢的用户 |
| `wechat-bridge-v*-integrated-win64.zip` | 网盘 | 源码与全部依赖已经合并 | 希望一次下载、解压后直接配置的用户 |

依赖包和整合版适用于 **Windows x64、Python 3.10–3.14**，不包含 Python 解释器本身。

## GitHub 源码版配合网盘依赖包

1. 把 GitHub 源码 ZIP 解压到一个新的空文件夹。
2. 把依赖 ZIP **直接解压到同一个文件夹**，不要再套一层目录。
3. 最终应能看到 `offline-packages` 文件夹与 `first.bat` 位于同一级。
4. 双击 `first.bat`，选中 Python 并进入“创建或修复独立环境”。
5. 确认前检查最终执行计划：必须显示“本地完整依赖包”和“不会下载 Python 包”。

## 网盘整合版

整合版已经包含 `offline-packages`，直接解压到新文件夹后运行 `first.bat`。启动器仍会先展示完整计划，只有用户确认后才创建环境或运行 pip。

## 完整性与失败处理

- 启动器会核对依赖包与当前 `requirements.txt` 是否属于同一版本。
- 每个 wheel 都记录文件大小与 SHA-256，安装前逐个校验。
- 离线目录缺文件、遭修改、混入未登记 wheel 或版本不匹配时会明确报错并停止，不会悄悄改成联网安装。
- 有效离线包会给 pip 增加 `--no-index`，从程序层面禁止本次安装访问在线 Python 软件源。
- 官方发布信息应同时公布三个 ZIP 的文件大小和 SHA-256；网盘上传完成后，再把依赖包和整合版的真实链接填写到 GitHub 发布页。

如果不放置 `offline-packages`，源码版会在用户确认后按原流程联网安装依赖。
